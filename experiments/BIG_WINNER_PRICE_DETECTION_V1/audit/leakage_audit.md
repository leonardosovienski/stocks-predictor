# Leakage audit

`signal_stage.py` rejeita caminhos de outcome e possui teste AST que proíbe imports de `outcome_stage` e `evaluation_stage`. Labels exigem manifest de sinais válido. O controle positivo injeta o label no ranking e deve produzir enriquecimento; a permutação negativa preserva cada tamanho de seleção mensal.
