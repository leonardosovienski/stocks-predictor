# Roadmap

Only unavoidable future/external items remain:

- mature frozen cohorts through 12 calendar months;
- collect a versioned external sector/security-master source before any V3 sector-relative research;
- schedule shadow execution externally if continuous runtime is desired (`READY_NOT_SCHEDULED` today);
- optionally research V3 only under a new identity and freeze.

No actionable P0/P1 implementation item is deferred here.
