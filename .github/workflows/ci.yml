name: CI Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  quality:
    name: Quality / Python 3.13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.12.1"
          enable-cache: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install project and dev dependencies
        run: uv sync --all-extras --python 3.13
      - name: Ruff
        run: uv run ruff check src tests main.py
      - name: Pyright
        run: uv run pyright
      - name: Tests
        run: uv run coverage run -m pytest -q
      - name: Coverage report
        run: uv run coverage report --fail-under=0
      - name: Build wheel
        run: uv build
      - name: Wheel smoke outside checkout
        run: |
          uv venv /tmp/stocks-wheel --python 3.13 --seed
          /tmp/stocks-wheel/bin/pip install dist/*.whl \
            "predictor-core @ https://github.com/leonardosovienski/core-predictor/releases/download/v2.3.0/predictor_core-2.3.0-py3-none-any.whl" \
            "predictor-ops @ https://github.com/leonardosovienski/predictor-ops/releases/download/v3.1.0/predictor_ops-3.1.0-py3-none-any.whl"
          /tmp/stocks-wheel/bin/python -c "import predictor_core, predictor_ops, src.rj_episodes, src.rj_judge"

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
